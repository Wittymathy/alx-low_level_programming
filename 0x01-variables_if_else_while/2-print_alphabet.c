#include <stdio.h>

/**
 * main - prints alphabets in lower case
 * Return: 0
 */

int main(void)
{
	char lowcase = 'a';

	while (lowcase <= 'z')
	{
		putchar(lowcase);
		c++;
	}

	putchar('\n');
	return (0);
}
