#include <stdio.h>
#include <stdlib.h>

/**
 * main - adds one line of code
 * Return: Always 0
 */

int main(void)

{
	int b;
	int a[5];
	int *p;

	a[2] = 1024;
	p = &b;

	printf("a[2] = %d\n", a[2]);

	return (0);
}
